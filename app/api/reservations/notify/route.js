import { NextResponse } from "next/server";

export async function POST() {
  // Email notifications via Appwrite Messaging — Phase 5
  return NextResponse.json({ error: "Not implemented yet." }, { status: 501 });
}
